#include "controle.hpp"
#include "Arvore.cpp"
#include "Nodo.cpp"

Nodo instancia(int valor) {
    Nodo raiz(valor);
    return raiz;
}

Arvore treeInstancia(int ind) {
    Arvore arvere(ind);
    return arvere;
}